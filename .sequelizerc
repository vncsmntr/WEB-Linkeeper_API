require('dotenv').config();
const path = require('path');

module.exports = {
  'config-path' : path.resolve('src/config', 'config.js'),
  'models-path' : path.resolve('src/models', 'models' ),
  'migrations-path' : path.resolve('src', 'migrations')
}